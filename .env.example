# This is an example of a .env file for a Cloudflare DNS record update script.
# Make sure to replace the values with your actual Cloudflare API token, zone ID, DNS record ID, and DNS record name.
API_TOKEN=your_api_token
ZONE_ID=your_zone_id
DNS_RECORD_ID=your_dns_record_id
DNS_RECORD_NAME=example.com